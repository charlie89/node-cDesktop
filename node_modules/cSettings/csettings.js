(function(context) {
	var json = module.exports = require(__rootpath + "/config.json");
	
	//handle port on hosting portals
	json.port = process.env['app_port'] || json.port;
	
	//set global __debug
	__debug = json.debug;

})(exports);